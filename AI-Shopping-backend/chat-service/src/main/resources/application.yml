server:
  port: 8085

spring:
  application:
    name: chat-service
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
  cloud:
    nacos:
      discovery:
        server-addr: 10.200.97.197:8848

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

chat:
  memory:
    maxMessages: 10
