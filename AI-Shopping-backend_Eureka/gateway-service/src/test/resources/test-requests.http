### Gateway服务测试请求
### 使用方法：
### 1. 先执行 "2. 测试用户登录" 获取token，替换下方的 {{userToken}}
### 2. 执行 "3. 测试商家登录" 获取token，替换下方的 {{sellerToken}}
### 3. 后续请求直接使用变量，无需重复修改

# 变量定义 - 登录后替换为实际token值
@userToken = 0010c21c-fa71-41ec-be4c-97b26d0b07bf
@sellerToken = b779c236-8666-4299-97a1-22a36ddbefcb
@baseUrl = http://localhost:8080

### 1. 测试未认证访问（应返回401）
GET {{baseUrl}}/api/user/order/test
Accept: application/json

### 2. 测试用户登录（白名单，应成功）
# 登录后复制返回的token替换上方的 @userToken
POST {{baseUrl}}/api/user/auth/login
Content-Type: application/json

{
  "username": "user001",
  "password": "123456"
}

### 3. 测试商家登录（白名单，应成功）
# 登录后复制返回的token替换上方的 @sellerToken
POST {{baseUrl}}/api/seller/auth/login
Content-Type: application/json

{
  "username": "merchant001",
  "password": "123456"
}

### 4. 测试用户注册（白名单）
POST {{baseUrl}}/api/user/auth/register
Content-Type: application/json

{
  "username": "newuser",
  "password": "123456",
  "phone": "13800138001"
}

### 5. 测试携带Token访问用户信息
GET {{baseUrl}}/api/user/auth/info
satoken: {{userToken}}
Accept: application/json

### 6. 测试IP限流（快速发送多次，第31次应返回429）
GET {{baseUrl}}/api/user/auth/login
Accept: application/json

### ==================== 用户端接口测试 ====================

### 7. 用户查询所有商品
GET {{baseUrl}}/api/user/product/all
satoken: {{userToken}}
Accept: application/json

### 8. 用户根据ID查询商品
GET {{baseUrl}}/api/user/product/1001
satoken: {{userToken}}
Accept: application/json

### 9. 用户搜索商品
GET {{baseUrl}}/api/user/product/search?name=手机
satoken: {{userToken}}
Accept: application/json

### 10. 用户下单
POST {{baseUrl}}/api/user/order/place
Content-Type: application/json
satoken: {{userToken}}

{
  "productId": "1001",
  "quantity": 1,
  "contactId": 1
}

### 11. 用户查询订单列表
GET {{baseUrl}}/api/user/order/list
satoken: {{userToken}}
Accept: application/json

### ==================== 商家端接口测试 ====================

### 12. 商家查询所有商品
GET {{baseUrl}}/api/seller/product/all
satoken: {{sellerToken}}
Accept: application/json

### 13. 商家创建商品
POST {{baseUrl}}/api/seller/product/create
Content-Type: application/json
satoken: {{sellerToken}}

{
  "name": "新商品",
  "price": 99.99,
  "stock": 100,
  "tags": "电子",
  "description": "商品描述"
}

### 14. 商家查询订单列表
GET {{baseUrl}}/api/seller/order/list
satoken: {{sellerToken}}
Accept: application/json

### 15. 商家按状态查询订单
GET {{baseUrl}}/api/seller/order/status/PENDING
satoken: {{sellerToken}}
Accept: application/json

### ==================== 废弃接口（向后兼容） ====================

### 16. 【废弃】原商品接口（测试向后兼容）
GET {{baseUrl}}/product/all
Accept: application/json

### 17. 【废弃】原订单接口（测试向后兼容）
GET {{baseUrl}}/order/list
Accept: application/json
